# City Flow 仕様書

作成日：2026-09-21  
構成：v0.1＝MVPの全体仕様／v0.2＝v0.1からの差分／v0.3＝v0.2からの差分

## 本書の読み方

**v0.1は、簡易3D空間内でGround配線のUXを検証するMVPとする。** 都市の表示とカメラは3Dだが、NodeとLineは地面上に限定する。手動経路調整は見下ろしカメラで行う。高さ方向の配線はv0.2、PLATEAUデータの利用はv0.3で導入する。

| バージョン | 位置付け | 都市空間・配線範囲 | 接続制約 | Lineの容量 |
| --- | --- | --- | --- | --- |
| v0.1 | 配線UXと輸送ルールのMVP | 簡易3D都市・Ground配線のみ | IN／OUTの接続本数 | 全Line共通の固定値 |
| v0.2 | 高さ方向・接続能力の拡張 | 簡易3D都市・高さ方向の配線を追加 | IN／OUTのPort Unit | Widthに応じた値 |
| v0.3 | 実在都市への展開 | PLATEAU都市モデルを追加 | v0.2を継承 | v0.2を継承 |

v0.2とv0.3は差分仕様として読む。明記した追加・変更以外は、それぞれ直前のバージョンを引き継ぐ。

Lv1は独立したステージではなく、同じ都市シーン内で始まる最初のWaveを指す。以後もシーンと配線を維持し、Nodeが徐々に追加される。本書でいう都市ステージは地形・マップの種類であり、Waveごとに切り替える面ではない。

元の会話で未確定だった挙動を整理するため、本書で置く案には「補完案」、調整用の数値には「暫定値」と記載する。いずれも既存の合意事項とは区別する。

## 用語

| 用語 | 意味 |
| --- | --- |
| FLOW | 目的のSink色を持つ、個々の輸送対象 |
| Node | FLOWの保持・中継とLineの接続を行う地点 |
| Source | FLOWを生成するNode |
| Sink | 自分の色と一致するFLOWを消化するNode |
| Relay | 生成・消化を行わず中継するNode |
| Buffer | Nodeで待機しているFLOWの集合とその量 |
| Line | 2つのNodeを結ぶ有向経路。v0.1ではGround、v0.2から高さ方向も使用 |
| In-Flight | 始点Nodeから出発し、終点Nodeへの受け渡しをまだ終えていないFLOW。Line上で停止中のものも含む |
| Capacity／MaxInFlight | 1本のLineが同時に保持できるFLOW数の上限 |
| FLOW Routing | NodeがFLOWの次の接続先を選ぶ処理 |
| Line Routing | 障害物を避ける、Lineの幾何学的な経路生成 |
| Hub | 複数のLineを集めて分配する構成上の役割。独立したNode種別ではない |
| Port Unit | v0.2で導入する、Nodeの接続能力を計る単位 |
| Width | v0.2で導入する、Lineの容量とPort消費量を決める太さの段階 |
| 削除予約 | 新規FLOWの流入を停止し、既存In-Flightの排出完了後にLineを削除する状態。削除完了前は取消可能 |
| Wave／Lv | 同じ都市シーン内で進む難度の段階。Lv1は最初のWaveで、Node追加などにより徐々に進行する |

本書ではA → BのAを「始点／送り出し元」、Bを「終点／受け取り先」と呼ぶ。削除待ちを妨げる受け取り先のBufferとは、BのBufferを指す。

---

# v0.1 — MVP仕様

## 1. コンセプトと目的

簡易3D都市に増え続ける色付きFLOWを、対応するSinkへ送り続けるリアルタイム・ネットワークゲーム。

プレイヤーはNode同士を地面上の有向Lineで接続し、建物を避ける経路を調整しながら、FLOWが滞留しすぎないネットワークを構築する。目的は、SourceがOverloadによってGame Overに至るまでの時間をできるだけ延ばすこと。

MVPでは次の体験を検証する。

- 直接Sinkへつなぐ確実性と、短いLineで中継する構成の違い。
- Nodeの接続数制限、Buffer、Lineの距離・方向・容量によるボトルネック。
- 3D空間を見渡して接続先を探し、Ground上に配線するUX。
- OverviewからNode 360へ入り、自動経路を作り、見下ろしカメラで手動調整する操作。
- Relayの入力停止、Line上の滞留、上流のSourceへの混雑の波及と、削除予約を含む再配線。

Lineの建設費、作成本数の共通リソース、総延長の予算は設けない。接続可否はNodeのI/O上限とGround経路の成立によって決まり、長いLineの不利は輸送性能に現れる。

## 2. ゲーム空間

Unity上に、地面と直方体を中心とした建物から簡易3D都市を作成する。PLATEAUデータや実在都市の再現は使用しない。

ステージには建物間の通路、広い空間、地面上で迂回を必要とする障害物を含める。建物を立体表示しても、MVPでは建物の高さによる経路選択は扱わない。

- Nodeは共通のGround面上に配置する。屋上や空中のNodeは配置しない。
- LineはGround面に沿う経路とし、制御点の高さを固定する。
- 建物のFootprintを避け、地面上で左右に迂回する。上越し・上昇・下降は行わない。
- 自動経路生成と手動編集の両方に、同じGround制約を適用する。
- 表示上、Lineを地面からわずかに浮かせても、配線ルール上の高さの自由度は与えない。
- 建物内部、地中、ステージの通行可能領域外にはLineを通せない。

**補完案：** MVPでは都市の障害物形状をプレイ中に変更しない。難度はNode追加とFLOW生成量で上げる。空間の複雑さは初期配置とNodeの出現場所で表現する。

## 3. FLOW

FLOWは小さな発光粒子で表現する。各FLOWは1色を持ち、その色が到達すべきSinkを示す。色数は最大5色とする。

色の例：Red、Blue、Yellow、Green、Purple。

- FLOWはSourceで生成され、NodeのBufferとLine上を移動する。
- 同色Sinkに到着すると即座に消化され、処理成功として計上する。
- 異なる色のSinkへ到着しても消化されず、通常の中継対象となる。
- FLOWはネットワーク全体の経路探索や、目的Sinkへの到達可能性判定を行わない。
- 輸送を待つためのPulse、車両、時刻表は導入しない。出発可能になったFLOWからLineへ送る。

## 4. Node

### 4.1 共通機能

すべてのNodeは、3D位置、Buffer、Incoming Line、Outgoing Line、IN接続上限、OUT接続上限を持つ。Node種類と接続能力は別に扱う。

| 種類 | FLOW生成 | FLOW消化 | 中継 |
| --- | --- | --- | --- |
| Source | 一定間隔で生成 | なし | 可能 |
| Sink | なし | 自分の色のみ即時消化 | 他の色を中継可能 |
| Relay | なし | なし | 可能 |

### 4.2 Source

現在存在するSink色の中からランダムに色を選び、FLOWを生成する。Sourceの色は固定しない。生成したFLOWは自分のBufferに入る。

Sourceは他のNodeから到着したFLOWも中継できる。

### 4.3 Sink

各Sinkは1つのSink Colorを持つ。同色FLOWは到着時に消化し、Bufferに滞留させない。他の色はBufferへ受け入れ、Outgoing Lineへ転送できる。

### 4.4 RelayとHub

Relayは生成も消化も行わない。HubはSource、Sink、Relayのいずれでも構成できる。

現在存在するすべてのSink色へ直接Outgoing Lineを持つNodeを「完全Hub」と呼ぶ。ここに到着した各FLOWは対応色Sinkへ確実に振り分けられる。ただしLineの空き待ちは発生するため、完全Hubでも処理能力が無制限になるわけではない。

## 5. BufferとOverload

NodeのBuffer量は、次の収支で変化する。

```text
Bufferの増減 = Sourceでの生成 + Lineからの受け取り完了 − Lineへの出発 − Sinkでの消化
```

MVPでは全NodeのBuffer容量を原則共通にし、`Buffer >= MaxBuffer`をOverloadとする。**RelayのOverloadでは入力が止まり、SourceのOverloadがGame Overにつながる。**

### 5.1 Relayの入力停止と再開

- RelayのBufferが上限に達したら、Incoming Lineからの受け取りを停止する。
- 受け取れないFLOWはLine上に残し、RelayのBufferを上限以上に増やさない。
- Outgoing Lineへの送り出しは継続できる。入力停止はNode全体の動作停止ではない。
- Bufferに空きができたら、その空き分だけ入力を再開する。複数Lineから同時に到着しても合計で上限を超えない。
- Relayが長時間Overloadのままでも、それ自体ではGame Overにしない。

### 5.2 SourceのOverloadとGame Over

SourceがFLOWを送り出せずBufferにため続けるとOverloadになる。敗北判定はSourceだけを対象とする。

**補完案：** 既存案の猶予時間をSourceに限定して維持する。SourceのOverloadが連続して猶予時間以上続くとGame Overとし、`Buffer < MaxBuffer`に回復すればタイマーをリセットする。初期検証用の猶予は5秒とする。

**補完案：Source自身の生成と外部入力**

SourceもBufferが満杯ならLineからの受け取りを停止する。一方、Source自身の生成は止めず、猶予中の生成分は超過Bufferとして保持する。FLOWを捨てたり、生成の自動停止だけで敗北を回避したりはしない。

### 5.3 Sinkが中継する色の扱い

**補完案：** Sinkは同色FLOWを即時消化し、異なる色に対してはRelayと同じBuffer入力制限を適用する。異色Bufferが満杯でも、受け渡し可能な位置にある同色FLOWは受け取って消化できる。Sink自身のOverloadは敗北条件にしない。

### 5.4 混雑の上流への波及

```text
RelayのBufferが満杯
  → Relayが入力停止
  → Incoming Line上のFLOWが停止・滞留
  → Lineの空き容量がなくなる
  → 上流Nodeが送り出せずBufferに滞留
  → SourceのOverloadが続くとGame Over
```

受け取りが完了した時点でのみ、FLOWをLineから取り除いてその容量を解放する。経路の終端に到達しただけでは、Nodeへの受け取り完了とは扱わない。

## 6. Nodeの接続制限

Nodeごとに次の値を持つ。

- `MaxIncomingConnections`：受け入れ可能なIncoming Line数。
- `MaxOutgoingConnections`：接続可能なOutgoing Line数。

v0.1では **1 Line＝1 Connection** とする。A → Bを作成するには、AのOUTとBのINに、それぞれ1本分の空きが必要。

接続上限は設定値として調整可能にする。すべての色へ直結する完全Hubには、少なくとも現在の色数分のOUT接続枠が必要になる。

Port UnitとLine Widthはv0.1には導入しない。

## 7. FLOW Routing

NodeはFLOWを送り出せるか評価する際に、現在Nodeと直接接続先だけを見る。

1. 現在Nodeが同色Sinkなら即座に消化する。
2. 同色Sinkへの直接Outgoing Lineがある場合、そのLineへ送る。
3. 同色Sinkへの直接Outgoing Lineがない場合、空き容量のあるOutgoing Lineからランダムに1本選び、送る。
4. 出発できるLineがない場合はBufferに残り、後で再評価する。

ここで利用できるOutgoing Lineは、新規FLOWの流入を受け付け、MaxInFlightに空きがあるLineとする。受け取り先Nodeが停止していても、Lineに空きがあればそのLineをバッファとして利用できる。削除予約中・経路切替待ちのLineは、新規FLOWの候補から外す。

**補完案：優先Lineが満杯の場合**

新規流入を受け付ける同色Sinkへの直結が存在していても、そのLineが満杯ならBufferで待つ。他の色のSinkやRelayへ迂回させない。同色Sinkへの直結が複数あれば、空きのある直結Lineから選ぶ。削除予約などで新規流入を停止したLineは、この直結判定からも外す。

**補完案：Buffer内の処理順**

待機時間の長いFLOWから出発可否を評価し、出発できないFLOWは残して、後続も評価する。ある色の出口が満杯でも、別の出口を使える色まで停止させない。ランダム選択は利用可能なLine間で等確率とし、出発する時点で決める。

### Routingが生む特徴

- 直接Sinkへの接続は、色を確実に処理方向へ送れる。
- 間接接続では、途中のNodeでランダムな分岐が起こる。
- 循環経路では同じNodeへ戻る可能性がある。過去の訪問履歴による回避は行わない。
- ネットワーク上でSinkへつながっているだけでは、到達時間や処理成功を保証しない。
- 出口が1本しかない中継Nodeでは選択が一意になる。中継するだけで必ずランダム性が生まれるわけではない。

Lineの自動経路生成は、ここで述べるFLOWのRoutingとは独立した機能である。

## 8. Lineの基本ルール

Lineは始点Nodeから終点Nodeへの、一方向のGround経路である。A → BのLineでは、FLOWはB → Aには進めない。

- Lineの作成本数に共通の上限や建設コストを設けない。
- 1本の長さや全Lineの総延長に、予算・リソースとしての上限を設けない。
- NodeのI/O上限と、通行可能なGround経路の存在は必要。
- FLOWが移動できるときの速度は、すべてのLineで共通にする。停止中は移動しない。
- すべてのLineでMaxInFlightを共通にし、長さでは増加させない。
- 満杯のLineへ出発できないFLOWは、出発NodeのBufferで待つ。

### 8.1 Line上の停止とバッファ機能

- 終点Nodeが入力を停止すると、受け取れないIn-Flight FLOWはLine上で停止する。
- 停止中FLOWもMaxInFlightを占有し続ける。消失、始点への自動返却、終点Bufferへの強制転送は行わない。
- 終点Nodeが受け取り可能になったらFLOWの移動・受け渡しを再開する。
- Lineは輸送路であると同時に、下流が詰まったときの固定容量バッファとして機能する。

**補完案：停止位置と順序**

FLOWはLineの終端側から順に待機し、後続は追い越さずに手前で停止する。新規FLOWは残り容量まで流入できるが、先行FLOWを追い越さない。途中から瞬間的に終点へ転送しない。Sinkの即時消化も、この受け渡し順を守る。

### 8.2 削除予約

Line削除はいつでも予約できるが、実際の削除は**In-Flightがすべて終点Nodeへ受け渡された後**に行う。満杯で動かないLineを即時削除してFLOWを取り除くことはできない。

1. A → Bの削除を予約すると、対象接続のA側OUT／B側INを削除待ちにし、そのLineへの新規FLOW流入を停止する。
2. Line上の既存FLOWは、Bが受け取れる範囲で排出する。
3. In-Flightが0になったらLineを削除し、AのOUTとBのINの接続枠を解放する。
4. 削除が完了するまでは、いつでも「削除予約をキャンセル」できる。

**補完案：OUT／IN停止の範囲**

停止は削除対象の接続に限定し、両Nodeの他のLineまで止めない。新規FLOWの流入を閉じる一方、B側INでは対象Line内に既に存在するFLOWの排出受け入れを継続する。既存FLOWの受け取りまで一律停止すると削除が完了しなくなるため、この排出を例外として扱う。

- BのBufferが満杯なら、FLOWはLine上にとどまり、削除予約も待機し続ける。時間経過による強制削除は行わない。
- 削除予約中も接続枠を占有する。接続枠が空くのは削除完了時だけとする。
- 予約取消時はLine、残存FLOW、その位置と接続枠を維持し、新規流入を再開する。Bの入力停止は取消では解消されない。
- 削除完了後は予約取消の対象外とする。

| 状況・操作 | Lineの状態 | 新規流入 | 接続枠 |
| --- | --- | --- | --- |
| 通常運行 | 運行中 | 空き容量まで受け付ける | 使用中 |
| 削除を指定 | 削除予約中 | 停止 | 使用中 |
| 予約中にBが満杯 | 削除予約中・受け取り待ち | 停止 | 使用中 |
| 削除前に予約取消 | 運行中へ復帰 | 空き容量まで再開 | 使用中 |
| 予約中のIn-Flightが0 | 削除完了 | 不可 | 解放 |

### 8.3 接続の形（補完案）

- 自分自身へのLineと、同一の始点・終点を持つ同方向の重複Lineは作成不可とする。
- A → BとB → Aは、別々の有向Lineとして作成でき、それぞれ接続枠と容量を使う。
- Line同士は交差可能とする。交差点でFLOWが合流・乗り換えすることはなく、接続はNodeでのみ成立する。
- Nodeの位置はプレイ中に移動・削除せず、Lineの接続と経路を編集対象とする。

## 9. Line Length・Travel Time・Capacity

Line Lengthは、Ground面上で建物を避ける経路全体の実長とする。v0.1では高さが固定なので、XZ平面上の距離を積算する。

折れ線経路の制御点を`P0 ... Pn`とした場合：

```text
L = Σ distanceXZ(Pi, Pi+1)  （すべての制御点の高さYは共通）
T = L / V
推定Throughput ≈ K / T = K × V / L

L：Line Length [m]
V：FLOW Speed [m/s]
T：停止がない場合のTravel Time [s]
K：MaxInFlight [FLOW]
推定Throughput：[FLOW/s]
```

描画を曲線にする場合も、移動・長さ計測・衝突判定が参照する実経路を一致させる。

推定Throughputは、送り出すFLOWが十分にあり、終点Nodeが滞りなく受け取り、空きができるたびに補充できる場合の輸送能力の目安。実際の到着時間は`L / V`にLine上の停止・受け取り待ち時間が加わり、実効Throughputは低下する。受け取りが止まっている間は、終点への排出量は0になる。

例として`K = 10 FLOW`、`V = 20 m/s`の場合：

| Line Length | Travel Time | MaxInFlight | 推定Throughput |
| --- | --- | --- | --- |
| 100 m | 5 s | 10 FLOW | 2 FLOW/s |
| 500 m | 25 s | 10 FLOW | 0.4 FLOW/s |

長いLineはFLOWが容量を長時間占有するため、出発NodeのBufferを継続的に減らしにくくなる。長いLineを作っても、より大きなFLOW置き場にはならない。

短いLineをRelayでつなぐ構成は各区間の回転を速くできる一方、中継NodeのBuffer・接続枠と分岐が新たな制約になる。Relayをプレイヤーが自由に増設する機能はMVPに含めない。

## 10. 自動Ground経路生成

Node Aと接続先Bを指定すると、A → Bの初期経路を自動生成し、確定前のPreviewとして表示する。

優先順位は次の通り。

1. 障害物を貫通せず、通行可能領域内に収まること。
2. Line Lengthが短いこと。
3. 不必要な折れ曲がりが少ないこと。

探索対象は、Ground面上の迂回と建物間の通過に限定する。建物の上空は候補に含めない。MVPで提示する自動経路は1候補とし、厳密な最短経路の保証までは求めない。

**v0.1実装上の採用（Step 06）：** 簡易都市では、クリアランス付き建物の角を用いた可視グラフ＋A*で1候補を生成する。全区間検証を確定Lineと共通化し、直線接続と不要な制御点の除去を優先する。詳細・暫定値は `docs/Architecture.md` に記録する。

Previewには、経路、始点・終点、方向、Line Length、Travel Timeを表示する。推定Throughputと、始点OUT／終点INの使用状況も確認できるようにする。

**補完案：判定と失敗表示**

- 制御点だけでなく、制御点間の全区間を障害物判定する。
- 障害物からのクリアランスは共通の設定値を用いる。Ground上の接続点には、必要な空間を確保する。
- 自動探索が失敗した場合は「自動経路を生成できません」と表示する。経路が絶対に存在しないと断定しない。
- 失敗時も手動で制御点を置けるが、経路全体の妥当性検証に成功するまで確定できない。
- I/O上限超過、障害物との衝突、通行可能領域外など、確定できない理由を個別に表示する。

## 11. 見下ろしカメラでの手動経路編集

自動生成した経路は、確定前にも確定後にも調整できる。**手動調整はOverviewの見下ろしカメラで行い、Node 360では制御点を編集しない。**

- 制御点を追加・移動・削除できる。
- 制御点はGround面のXZ方向へ移動できる。Y方向は固定し、高さの編集操作は設けない。
- 始点と終点はNodeに固定し、経路編集で接続先は変更しない。
- 建物の左・右への迂回や、地面上の隙間への通し直しができる。
- 編集中はLine Length、Travel Time、推定Throughputを随時更新する。
- 無効な区間を色と理由で示し、無効な経路は適用できない。
- 「適用」「取消」「自動経路を再生成」を用意する。

**採用済み補完案（Step 09）：運行中のLineを編集する場合**

FLOWの消失や、経路の短縮による瞬間移動を避けるため、以下の切替手順を採用する。

1. 編集中のPreviewは実際の運行に影響させない。取消時は元のLineを維持する。
2. 「適用」後は、そのLineへの新規出発を停止する。既存FLOWは旧経路上に残し、終点Nodeが受け取れるときに排出する。
3. In-Flightが0になった時点で新経路へ切り替え、出発を再開する。
4. 待機中は「経路切替待ち」と残りIn-Flight数を表示する。接続枠は維持する。

終点Nodeの入力停止中は経路切替も待機する。切替完了前は適用待ちを取消し、旧経路の運行へ戻せるようにする。Line削除は§8.2の予約・排出・取消ルールに従う。

**採用済み補完案（Step 09）：** 経路切替待ちと削除予約は同時に適用せず、現在の予約を取り消してから別の操作を行う。

## 12. カメラ

### 12.1 Overview Camera

都市全体を上空から見下ろす通常モード。真上の視点に加え、3D都市の形状とGround配線が読める斜め俯瞰を可能にする。手動経路調整を開始すると、Ground面の制御点を扱いやすい見下ろし視点に切り替える。

操作はPan、Zoom、Orbit、Node選択、Line選択、Ground経路の制御点編集。全体の接続構造、Bufferの偏り、Lineの方向、FLOWの流れと停止位置を把握するために使用する。

選択Nodeへのフォーカスと、都市全景へ戻る操作を用意する。

### 12.2 Node 360 Camera

選択Nodeを基点とした配線用モード。カメラはNode付近に置き、水平方向に360°、上下方向にも視線を動かして周囲を見渡せる。

近いNode、遠いNodeを、実際の3D空間内で選べることを目的とする。MVPのNodeはすべてGround面上にあり、高低差を使った接続先探索は行わない。カメラやNode自身の表示が候補選択を妨げないようにする。

Node 360は接続先探索と経路Previewに用い、手動調整を選ぶとOverviewの見下ろしカメラへ移る。切替時は選択中の始点・終点とPreview経路を保持する。配線を終えたときには元のOverview位置へ戻れるようにする。

## 13. Node 360での接続先探索

周辺NodeをNear／Mid／Farなどの距離帯で識別・絞り込みできるようにし、数値の距離も表示する。距離帯の閾値はステージ規模に合わせて調整する。

距離は用途を区別する。

| 表示 | 意味 | 表示タイミング |
| --- | --- | --- |
| 直線距離 | 始点から候補NodeまでのGround面上の直線距離 | 候補探索時 |
| Line Length | 障害物を避けた実際のGround経路長 | 経路生成後・編集中 |

候補Nodeには種類、Sink Color、距離、IN／OUTの使用数と上限を表示する。接続可否に直接関係する始点OUTと候補INを強調する。

**補完案：候補の見つけやすさ**

- 画面外Nodeは方向インジケータで示し、距離帯の絞り込みを反映する。
- 建物に隠れたNodeも遮蔽中のマーカーとして選べる。建物越しに見つかることと、Lineが建物を通れることを混同させない。
- ラベルが重なる場合は、注目中の候補を優先表示する。
- 経路探索前は「接続枠あり／経路未確認」と表示する。枠が空いているだけで接続可能と確定しない。
- 接続不可のNodeも選択して理由を確認できる。

**v0.1実装上の採用（Step 07）：** 上記の候補表示に関する補完案を採用する。ラベル重複時も候補順送りで注目・選択できるようにする。距離帯とカメラ操作の暫定値は `docs/Architecture.md` に記録する。

## 14. 配線UX

基本フローは次の通り。

1. **始点選択：** OverviewでNode Aを選び、「Connect」を実行する。
2. **周囲の探索：** Node 360へ切り替え、距離帯と方向を見ながら候補を探す。
3. **終点選択：** Node Bを選択する。接続方向はA → Bとする。
4. **自動生成：** 初期Ground経路を生成し、3D都市の地面上にPreview Lineを描く。
5. **確認・調整：** 経路、Line Length、Travel Time、方向、I/O使用状況を確認する。手動調整する場合は見下ろしカメラへ切り替え、Ground面上の制御点を操作する。
6. **確定：** 経路と接続枠を再検証し、成立していればLineを生成する。ここで初めて接続枠を使用し、FLOWが出発可能になる。
7. **後から編集：** 生成済みLineを選び、見下ろしカメラで経路編集するか、削除予約を行う。予約中は残りFLOWと停止理由を確認し、必要なら予約を取り消す。

Previewは本線と区別できる半透明表示などを使用し、方向矢印を表示する。選択経路の強調、遮蔽区間の補助表示、Ground上の制御点表示によって「なぜこの長さになるか」を読めるようにする。

Preview例（`V = 20 m/s`、`K = 10 FLOW`）：

```text
始点 → 終点：Source A → Red Sink
Line Length：284 m
Travel Time：14.2 s（停止・受け取り待ちを除く）
推定Throughput：約0.70 FLOW/s
始点OUT：使用数／上限 → 確定後の使用数／上限
終点IN ：使用数／上限 → 確定後の使用数／上限
状態：確定可能
```

取消は経路や接続枠を消費しない。確定できない場合は理由を表示し、編集・別候補選択へ戻れるようにする。

### 14.1 Escによる時間停止

Escでゲーム時間を停止する。停止中も、カメラ操作、Node／Lineのホバー詳細、経路Preview、手動調整、削除予約とその取消を操作できるようにする。

- 停止対象はFLOW生成・移動・Nodeとの受け渡し、Node追加、Wave進行、SourceのOverloadタイマーなど、シミュレーション全体とする。
- In-Flightを排出する処理も止まるため、FLOWが残るLineの削除予約・経路切替は再開後まで進まない。
- Escは経路編集の取消キーとして兼用しない。編集や削除予約の取消は明示的な操作を用意する。

**採用済み補完案（Step 10）：** もう一度Escを押すと再開する。カメラ切替・ホバー・編集開始だけでは自動停止しない。In-Flightが既に0のLineの削除など、FLOWの時間進行を必要としない操作は停止中にも完了できる。

## 15. ホバー詳細と状態の可視化

**NodeまたはLineにマウスカーソルが当たると、クリックしなくても詳細情報を表示する。** OverviewとNode 360の両方で使え、ゲーム時間停止中も確認できるようにする。

| 対象 | 通常時 | ホバー時・選択時・警告時 |
| --- | --- | --- |
| FLOW | 色付き発光粒子の移動 | 色と流れる方向を識別可能にする |
| Node | 種類・Sink色・Buffer使用率 | 種類、Buffer数／上限、色別内訳、IN／OUT使用数、入力停止の有無 |
| Source | 生成とBufferの状態 | 生成間隔、Overload中はGame Overまでの残り猶予時間 |
| Relay／中継中のSink | Overload・入力停止の警告 | 受け取り待ちのIncoming Lineと、詰まりを解消するために確認すべき出力接続 |
| Line | 方向矢印・移動／停止中FLOW | 始点→終点、長さ、停止なしの移動時間、In-Flight／上限、停止数、推定Throughput、停止理由 |
| 削除予約Line | 通常Lineと区別した表示 | 削除予約中、残りFLOW数、受け取り先の状態、予約取消操作 |
| 経路Preview | 本線と区別した経路表示 | 有効／無効と理由、編集制御点 |

Buffer使用率はリングやゲージで示し、FLOWの目的色と混同しない表現にする。長いLineでは粒子密度が低くても満杯になり得るため、密度だけで混雑を判定させず、容量使用率も示す。

停止中FLOWは動きを止め、通常運行と区別できる表示にする。削除予約中かどうかと、受け取り先が詰まっているかどうかは別々に表示し、両方が同時に成立することを伝える。受け取り先が満杯で排出できない場合、削除までの残り秒数は断定せず「受け取り先のBuffer空き待ち」と表示する。

基本HUDには現在のWave、経過時間、累積処理成功数、SourceのOverload警告、Relay等の入力停止数、Pause状態を表示する。多数のLineがある場面では、ホバー中・選択中のNodeの入出力とLineを強調する。

## 16. Wave進行・Node追加・Game Over

### 16.1 同一シーンでの連続進行

ゲーム開始時のLv1を最初のWaveとする。都市シーンを切り替えず、同じネットワークを運行・拡張しながら次のWaveへ進む。

- Waveが変わっても、既存Node、Line、Buffer、In-Flight、削除予約などの状態を維持する。
- Nodeはプレイ中に徐々に追加する。Wave切替で既存の配置を作り直さない。
- Wave間にステージ選択、クリア画面、配線のリセット、FLOWの一括消化を挟まない。
- 「各色を何個届けたらLv1クリア」といった配送数による面クリア条件は設けない。
- 最初のWaveは少数Nodeと低い生成速度から始め、接続・観察・調整に慣れる時間を確保する。

**補完案：** Wave更新はHUDと短い通知で伝え、時間を自動停止しない。新Nodeは出現地点の強調や画面外インジケータで知らせる。Waveを進める具体的な時間・追加Node構成・難度上昇量は、プレイ検証で決める。

### 16.2 Nodeの段階的な追加

ゲーム進行に伴ってSource、Relay、Sinkを徐々に追加し、Source数、生成速度、色数を増やす。色数は最大5色。出現方法と具体的なタイミングの決定方式は、以下のレベルデザイン上の検討事項とする。

- Sourceが生成する色には、必ず1つ以上の対応Sinkが存在する。
- 新色は、その色のSinkが出現した後に生成対象へ加える。
- 序盤は少数Node・2色・低生成速度から開始し、中盤以降に3〜5色とFLOW量を増やす。
- 新Nodeの出現位置が、Ground面での接続先・迂回経路・Hub構成の新しい判断を生むようにする。

**検討事項：Nodeの追加方法とレベルデザイン（採用方式は未決定）**

Nodeがプレイ中に追加されることと、配置・種類・出現順をアルゴリズムで自動生成することは区別する。現時点では、次の可能性を挙げるにとどめる。

- **手動で設計する方式：** 制作者が出現位置、Node種類、Sink色、接続能力、出現順・タイミングを調整し、用意した内容をプレイ中に順次出現させる。
- **自動生成する方式：** 空間の空き、既存Nodeとの距離、Wave、ネットワークの負荷などを候補条件として、アルゴリズムが追加内容を選ぶ可能性がある。参照する条件や選び方は未定とする。
- **組み合わせる方式：** 制作者が出現候補地点や追加パターン、難度の範囲を用意し、その中から自動選択する可能性がある。

ここでいう手動調整は制作者によるレベルデザインであり、プレイヤーがNodeを自由配置する機能ではない。初回の分かりやすさ、難度の調整しやすさ、繰り返し遊ぶ際の変化を踏まえて検討し、本書では方式・アルゴリズム・採用順を決定しない。自動生成はMVPの必須要件にも加えない。

**補完案：出現時の成立条件**

NodeはGround面上で、障害物内や通行不能な孤立空間を避け、少なくとも既存NodeへのGround経路を持つ位置に出現させる。新Sourceには生成開始までの猶予を設ける。接続枠が使用済みの場合は、プレイヤーによる再配線を必要としてよい。

Lineが無料であることだけでは、I/Oの空きやネットワーク全体の処理能力は保証しない。出現間隔と猶予時間は、経路作成・再配線が可能な範囲で調整する。

### 16.3 Game Over

§5.2の補完案に従い、いずれかのSourceでOverloadが連続して猶予時間以上続いた場合、Game Overとする。Relay・SinkのOverload、Lineの満杯、削除予約の待機自体は敗北条件にしない。

終了時には到達Wave、生存時間、累積処理成功数、原因となったSourceを表示する。

## 17. 調整パラメータ

以下の数値は**暫定値**であり、MVPのプレイ検証で変更する。

| 項目 | 初期検証用の値・方針 |
| --- | --- |
| 色数 | 開始時2色、最大5色 |
| MaxBuffer | 全Node共通で50 FLOW |
| MaxInFlight | 全Line共通で10 FLOW |
| FLOW Speed | 全Line共通で20 m/s |
| SourceのOverload猶予 | 5秒（補完案）。Relay・Sinkには敗北用タイマーを設けない |
| IN／OUT接続上限 | Nodeごとに設定。少ない上限と5色分の出口を持てる上限を比較 |
| Source生成間隔 | Sourceごとに設定し、進行に応じて短縮 |
| Node出現間隔・生成開始猶予 | 配線に必要な時間を計測して調整 |
| Nodeの追加方法 | 手動設計・自動生成・組み合わせを検討。採用方式とアルゴリズムは未決定 |
| Waveの進行条件・追加Node構成 | 同一シーン内で徐々に難しくなるよう調整。具体値は未定 |
| 距離帯・クリアランス・空間の上限 | ステージ規模に応じて設定 |

## 18. MVPの完了条件

- 簡易3D都市で、Ground面上のNode同士を接続できる。
- 自動・手動ともGround配線に限定され、建物のFootprintを迂回できる。
- OverviewとNode 360を切り替え、距離別に候補を探せる。
- 自動経路をPreviewし、見下ろしカメラでXZ制御点を修正して確定できる。
- 制御点が障害物の外でも、途中の区間が貫通していれば確定できない。
- 同色Sinkへの直結が優先され、直結がなければ空きLineへランダムに送られる。
- Line満杯時に出発Nodeで待機し、長いLineほど容量の回転が遅くなる。
- IN／OUTの上限を超えて接続できない。
- Escでシミュレーション全体を停止・再開でき、停止中も配線操作と情報確認ができる。
- NodeとLineをホバーすると、それぞれの詳細情報が表示される。
- RelayのBufferが満杯になると入力が止まり、出力によって空きができると受け取りが再開する。
- 受け取り先が停止したFLOWはLine上に残り、停止中もMaxInFlightを占有する。
- Relay・SinkのOverloadではGame Overにならず、SourceのOverloadが継続するとGame Overになる。
- 削除予約は新規流入を止め、In-Flightの受け渡し完了後にだけLineと接続枠を解放する。
- 受け取り先が満杯なら削除予約が完了せず、削除前に予約を取り消すと残存FLOWを維持して運行に戻る。
- 確定済みLineの編集・削除によってFLOWが消失せず、移動が飛ばされない。
- Wave進行とNode追加の際にシーンが切り替わらず、既存ネットワークとFLOWの状態を維持する。

プレイ検証では、完全Hubへの集中と分散構成、長距離直結と短距離中継、下流の詰まり解消と削除予約・取消を比較する。接続先探索から見下ろし編集への切替が分かりやすいことも確認する。

## 19. v0.1の対象外

- PLATEAUの読み込みと実在都市ステージ。
- 高さの異なるNode、Lineの上昇・下降・建物の上越し、Y方向の制御点編集。
- Node Port Unit、Line Widthの選択・変更、Width別Capacity。
- Lineの方向をその場で反転する操作。
- 建設費、ゲーム内通貨、総延長予算、Line本数リソース。
- Pulse、車両、固定高度レイヤー、専用の昇降設備。
- FLOWのグローバル経路探索や自動ネットワーク最適化。
- Nodeの購入・自由増設・移動・アップグレード。
- 1本で両方向を扱うBidirectional Line。

---

# v0.2 — v0.1からの差分仕様

本章に記載した変更箇所だけをv0.1へ適用する。

## Δ1. 高さ方向のNode配置・配線・経路編集を追加

**変更対象：v0.1 §2「ゲーム空間」、§8「Lineの基本ルール」、§9「Line Length・Travel Time・Capacity」、§10「自動Ground経路生成」、§11「見下ろしカメラでの手動経路編集」、§13「接続先探索」、§16のNode配置範囲**

簡易3D都市を引き続き使用し、Groundに限定していた配線を高さ方向へ拡張する。PLATEAUデータはこの段階では使用しない。

- 異なる高さにNodeを配置できるようにし、屋上や空中のNodeを接続対象に加える。
- Lineの上昇・下降、建物の上越しを可能にする。
- 障害物判定をFootprint中心の平面判定から建物ボリュームの3D判定へ拡張する。上昇・下降区間も含めて貫通を禁止する。
- 自動経路生成で、建物の横を迂回するか、上を越えるかを探索する。
- 固定のGround／Elevated／Skyレイヤーや専用の昇降設備は導入せず、制御点で高さを調整する。
- Node 360の候補情報に高低差を加え、直線距離を3D距離へ変更する。

Line Lengthの計算を、Ground上のXZ距離から、上昇・下降を含む3D経路長へ置き換える。

```text
L = Σ distance3D(Pi, Pi+1)
```

手動調整のカメラは引き続きOverviewの見下ろしカメラとし、XZの制御点移動にY方向の高さ調整を追加する。

**補完案：** 高さの変更には専用ハンドルまたは高さ入力欄を用い、斜め俯瞰でも結果を確認できるようにする。Node 360は探索・Previewに用い、手動編集はOverviewで行う。

追加検証：同じ簡易都市内で横迂回と上越しを比較でき、高さ方向の移動量がLine Length・停止なしのTravel Time・推定Throughputへ反映されること。

## Δ2. 接続本数制をNode Port Unit制へ置き換え

**変更対象：v0.1 §4「Node」、§6「Nodeの接続制限」**

`MaxIncomingConnections`と`MaxOutgoingConnections`による単純な本数制限を、次のPort容量へ置き換える。

- `InPortCapacity`：Incoming Lineに割り当てられるUnitの合計。
- `OutPortCapacity`：Outgoing Lineに割り当てられるUnitの合計。

LineのWidthに応じたPort Unitを、始点のOUTと終点のINの両方で使用する。Portは購入する通貨ではなく、Nodeが持つ接続能力とする。

```text
UsedOut(A) = Aを始点とするLineのPort消費量の合計
UsedIn(B)  = Bを終点とするLineのPort消費量の合計

A → Bの新設条件：
UsedOut(A) + PortCost(Width) <= OutPortCapacity(A)
UsedIn(B)  + PortCost(Width) <= InPortCapacity(B)
```

接続中はUnitを占有し、Lineの削除完了で解放する。削除予約中・受け取り待ちでもUnitは解放せず、予約取消では元の割当を維持する。接続本数の上限を別途重ねて適用しない。

NodeごとにPort容量の差を付けられるようにする。例としてIN／OUTが2／2、4／4、8／8のNodeを用意し、分岐や集中に向いた場所が変わるかを比較する。これらは暫定値であり、新しいNode種別やアップグレード制度の追加を意味しない。

## Δ3. Line WidthとWidth別Capacityを追加

**変更対象：v0.1 §8「Lineの基本ルール」、§9「Line Length・Travel Time・Capacity」、§17「調整パラメータ」**

全Line共通だったMaxInFlightを、Widthごとの値へ変更する。WidthはThin／Medium／Thickの3段階とし、設定表でCapacityとPort消費量を対応させる。

| Width | MaxInFlightの暫定値 | 始点OUTの消費 | 終点INの消費 |
| --- | --- | --- | --- |
| Thin | 5 FLOW | 1 Unit | 1 Unit |
| Medium | 10 FLOW | 2 Units | 2 Units |
| Thick | 20 FLOW | 3 Units | 3 Units |

この暫定表ではCapacityを単純なWidth番号の定数倍として定義しない。調整時も、容量表とPort消費表を一組として扱う。

変更後の性能計算では、v0.1の共通容量`K`を`K(Width)`へ置き換える。

```text
推定Throughput ≈ K(Width) × V / L
```

Widthを増やしてもFLOWの速度は変えない。長い迂回経路の輸送能力をWidthで補えるようになるが、停止なしの移動時間`L / V`は短縮されない。混雑による待機時間は、ネットワークの状態に応じて変化する。

**補完案：** v0.2のWidthは容量とPort消費を表す。太さによって必要な物理的通路幅を変える判定は導入しない。幾何学的なクリアランスは共通のままとし、狭い隙間を通る際は描画上も建物との貫通を避ける。

追加検証：OUTが6 UnitならThinを6本、Mediumを3本、Thickを2本、またはその組合せに配分できる。5色への完全Hubは最低5 Unitを出口に必要とし、細い出口を多数持つか、少数の幹線を太くするかという判断が生まれることを確認する。

## Δ4. 配線Previewと可視化へPort／Widthを追加

**変更対象：v0.1 §13「接続先探索」、§14「配線UX」、§15「ホバー詳細と状態の可視化」**

自動経路PreviewにWidth選択を追加する。Widthを変更するとCapacity、推定Throughput、始点OUT・終点INの必要Unitと確定後使用量を更新する。

追加する確認順序：

```text
自動経路Preview → Width選択 → 両端Port消費の確認 → 確定
```

- Node候補のI/O表示を、接続本数からPortの使用Unit／容量へ変更する。
- 選択中Widthで接続できない場合は、どちらのNodeのIN／OUTが何Unit不足するかを表示する。
- Node周囲のリングやスロットでPortの使用・空きを表現する。
- Lineの描画幅でWidthを示し、In-Flight量・容量使用率と区別する。
- Nodeのホバー詳細にPort使用量、Lineのホバー詳細にWidthと両端のPort消費量を追加する。

Previewの追加項目例：

```text
Width：Medium
MaxInFlight：10 FLOW
今回のPort消費：始点OUT 2 Unit／終点IN 2 Unit
始点OUT：2/6 → 4/6 Unit
終点IN ：1/4 → 3/4 Unit
```

## Δ5. 既存LineのWidth変更を追加

**変更対象：v0.1 §11「見下ろしカメラでの手動経路編集」、§14「配線UX」**

既存Lineを選択してWidthを変更できるようにする。変更前後のCapacity、推定Throughput、両端Portの使用量をPreviewする。

- 拡幅時は、既に使用中のUnitとの差分だけを追加で必要とする。
- 始点OUT・終点INのどちらかが不足していれば適用不可とする。
- 縮幅時は、新しいCapacityを超えたFLOWを削除しない。

**補完案：変更の適用方法**

拡幅は追加Unitを確保した上で適用する。縮幅で現在のIn-Flight数が新Capacityを超える場合、新規出発を止め、終点Nodeへの受け渡しによって新Capacity以下になるまで待つ。受け取り先が満杯なら切替は進まない。切替完了時に余ったPort Unitを解放する。待機中は旧WidthのPort消費を維持し、処理状況を表示する。

**補完案：** 縮幅の適用待ちは取消可能とし、削除予約や経路切替と同時に適用しない。

## Δ6. 自動経路の複数候補を追加

**変更対象：v0.1 §10「自動Ground経路生成」、§14「配線UX」**

自動生成結果を1候補から、最大2〜3候補の比較へ拡張する。候補の例は、最短優先、建物の横を回る経路、屋上を越える経路。

- 各候補にLine Length、Travel Time、選択中Widthでの推定Throughputを表示する。
- 有効な候補だけを提示する。同じ経路しか得られない場合は1候補でもよい。
- 候補名は探索方針を示し、厳密な最短経路を保証する名称にしない。
- 候補を選択した後に制御点で微調整できるようにする。

Δ1で導入する高さ方向の経路を含めて候補を比較する。

## Δ7. 既存Lineの方向反転を追加

**変更対象：v0.1 §8「Lineの基本ルール」、§11「見下ろしカメラでの手動経路編集」**

A → BのLineを、同じ経路のB → Aへ変更する操作を追加する。

- 反転後に使用するBのOUTとAのINのPort容量を検証する。
- 逆方向の同一接続が既に存在する場合は、重複になる反転を不可とする。
- 反転Previewには新しい方向と、両NodeのIN／OUT使用量の変化を表示する。

**補完案：運行中の反転**

新方向のPortを予約し、新規出発を停止する。旧方向のIn-Flightがすべて終点Nodeへ受け渡されてから、旧方向のPortを解放して反転を適用する。受け取り先が満杯なら反転も待機し、途中でFLOWの向きを変えて詰まりを解消することはできない。

待機中は旧方向の使用Unitと新方向の予約Unitを区別して表示する。反転待ちは取消可能とし、取消時は新方向の予約Unitを解放する。削除予約など他の変更待ちとは同時に適用しない。

## Δ8. v0.2の追加完了条件と保留範囲

v0.1の完了条件に、以下を追加する。I/Oに関する判定はPort Unit制へ読み替える。

- 簡易3D都市で高さの異なるNodeを接続し、横迂回と上越しを選べる。
- 見下ろしカメラで制御点の高さを調整でき、上昇・下降もLine Lengthへ反映される。
- Widthに応じて両端のPortを使用し、Unit不足の接続・拡幅・反転を確定できない。
- 同じ長さのLineで、Widthに応じてMaxInFlightと推定Throughputが変わる。
- 同じWidthなら、長いLineでもMaxInFlightが増えない。
- 縮幅・反転・削除で、停止中を含むIn-Flightの消失やPortの二重利用が起きない。
- 受け取り先が満杯のままでは縮幅・反転・削除に必要な排出が進まず、適用待ちを取り消せる。
- 自動経路の候補を比較し、選択後に手動調整できる。
- 多数の細線、少数の太い幹線、複数Hubへの分散に、都市形状とPort容量に応じた選択理由が生まれる。

v0.2でも、Widthに応じた物理的通路幅の制限、Nodeのアップグレード、Bidirectional Lineは保留する。Capacity／Port消費の最終バランスは実装・プレイ検証で決定する。PLATEAUデータの利用はv0.3へ送る。

---

# v0.3 — v0.2からの差分仕様

## Δ1. PLATEAUによる実在都市ステージを追加

**変更対象：v0.2 Δ1の簡易3D都市と、その自動3D経路生成で使用する障害物データ**

簡易3D都市に加え、PLATEAUの都市モデルを使用するステージを追加する。建物のFootprint、高さ、ボリュームを、Line Routingの障害物形状へ反映する。

- 高層ビル街、低層住宅地、建物密集地、開けた市街地によって、経路とLine Lengthが変わるようにする。
- Node配置はゲーム側で設計してよい。PLATEAUからの自動Node配置は必須にしない。
- 表示用モデルと経路・衝突判定用形状を分けられる構成とし、判定形状を簡略化しても、見えている建物をLineが貫通しないようにする。
- 取り込み範囲、座標変換、距離の単位をそろえ、Line Lengthをm単位で扱えるようにする。
- 比較検証用として、v0.1・v0.2の簡易3D都市も残す。

対象都市、利用するデータの範囲・詳細度、取り込み方法、性能目標は、データを用いた実装検証時に決定する。

## Δ2. v0.3の追加完了条件

- PLATEAU建物を障害物として、実際の高さと迂回がLine Lengthへ反映される。
- 都市形状の違いによって横迂回・上越し・中継構成・Width配分の有利不利が変化する。
- 取り込み後も距離の単位が一致し、Travel Timeと推定Throughputが正しく算出される。
- 実在都市でも、ホバー詳細、見下ろし編集、Node 360での候補探索が利用できる。

PLATEAUが背景だけでなく、経路長と輸送性能を左右する盤面になっていることを検証する。

---

参照元：[PLATEAUアプリ案の提案](chatgpt-conversation://6aa4b257-0cb8-83ee-95f6-4cc6f0b62354)
